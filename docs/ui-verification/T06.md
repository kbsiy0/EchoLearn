# UI Verification Report — T06: Frontend Router + Directory Reshuffle

**Date**: 2026-04-17
**Branch**: change/phase0-refactor
**HEAD**: f4f4b9a
**Verdict**: PASS

---

## Environment

| Component | Status |
|---|---|
| Backend (`http://localhost:8000`) | Running (FastAPI + SQLite) |
| Frontend (`http://localhost:5173`) | Running (Vite dev server) |
| Test video (cache-hit) | `dQw4w9WgXcW` (pre-existing in DB) |

---

## Golden Path Steps

### 1. Load HomePage (`/`)
- **Expected**: URL input + history list rendered
- **Result**: PASS — input disabled until URL entered; history shows `dQw4w9WgXcW`

### 2. Submit YouTube URL
- **Input**: `https://www.youtube.com/watch?v=dQw4w9WgXcW`
- **Expected**: `POST /api/subtitles/jobs` → cache-hit → immediate navigate to `/watch/:videoId`
- **Result**: PASS — immediately navigated to `/watch/dQw4w9WgXcW`

### 3. PlayerPage (`/watch/dQw4w9WgXcW`)
- **Expected**: YouTube iframe + subtitle panel rendered; title shown in header
- **Result**: PASS — iframe loaded; "字幕 (0 句)" subtitle panel shown (test data has empty segments); title "t" in subheader
- **Screenshot**: `T06-player-page.png`

### 4. Navigate back to HomePage via header link
- **Expected**: Click "EchoLearn" → return to `/`
- **Result**: PASS — URL changed to `/`, URL input restored

### 5. Click history item → navigate to PlayerPage
- **Expected**: Click history button → navigate to `/watch/dQw4w9WgXcW`
- **Result**: PASS — navigated correctly

---

## Routing Verification

| Route | Expected | Actual |
|---|---|---|
| `/` | `<HomePage />` | PASS |
| `/watch/dQw4w9WgXcW` | `<PlayerPage />` | PASS |
| Header "EchoLearn" link → `/` | navigate back | PASS |
| History item click → `/watch/:videoId` | navigate to player | PASS |

---

## p95 Sync Accuracy (Informational Only — T07 gate)

Not measured in T06 — test video segments were empty. T07 will establish sync benchmarks with real Whisper-transcribed content.

---

## Issues Found During Verification

1. **API field name mismatch**: `createJob` was sending `{ youtube_url }` but backend expects `{ url }`. Fixed in commit `f4f4b9a`.
2. **Test video `jNQXAC9IVRw` ("Me at the zoo") is unavailable** on YouTube — used cached `dQw4w9WgXcW` instead for the golden path.

---

## Console Errors During Test

- 0 JS errors after API fix
- 2 React Router future-flag warnings (expected — not errors)

---

## Final Test Suite

```
Test Files  3 passed (3)
Tests       27 passed (27)
```

All 6 `useJobPolling` tests now run against the real hook (placeholder deleted).
